import { Layout, LayoutBody, LayoutHeader } from '@/components/layout';

const Dashboard = () => {
    return (
        <Layout className="h-full">
            <LayoutHeader>222</LayoutHeader>

            <LayoutBody>sss</LayoutBody>
        </Layout>
    );
};

export default Dashboard;
